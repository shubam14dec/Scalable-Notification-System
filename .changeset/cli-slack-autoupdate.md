---
"@asyncify-hq/cli": minor
---

`asyncify dev` now auto-updates a Slack app's Event/Interactivity URLs on tunnel rotation when the connection holds a config refresh chain, with a graceful paste-table fallback for legacy or expired connections.
