---
'@asyncify-hq/react-native': minor
---

First release: `usePushRegistration` hook that registers a device's FCM token against an Asyncify subscriber for native push on Android and iOS — handling the notification permission, token rotation (idempotent re-register), device removal, and foreground-message delivery via React Native Firebase.
