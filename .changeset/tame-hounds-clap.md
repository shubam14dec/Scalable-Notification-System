---
'@asyncify-hq/agent': patch
---

Document the buttons + onAction API (shipped in 0.2.0) in the README — tappable choices with `ctx.reply(text, { buttons })`, clicks handled in `onAction`, per-channel rendering notes.
