---
'@asyncify-hq/cli': minor
---

First release: `asyncify dev` (managed cloudflared tunnel with automatic PUBLIC_URL rotation and webhook rewiring) and `asyncify create-agent` (bridge-agent starter scaffolder).
