# Native projects — regenerated by `expo prebuild` / EAS, never committed.
android/
ios/

node_modules/
.expo/
dist/
web-build/

# Firebase config — each developer downloads their OWN from the Firebase
# console (see README). Never commit it.
google-services.json
GoogleService-Info.plist

# npm pack fallback tarball (see README)
*.tgz

# misc
*.log
.DS_Store
